<% common_parameters = "--verbose --strict --no-source --color --format pretty --require features --require support --require lib --expand" %>

default: <%= common_parameters %> --tags ~@wip
local:   <%= common_parameters %>
